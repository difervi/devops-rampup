# Environment file for movie-analyst
# Replace or populate these with secure values in production
DB_HOST={{ db_host | default('REPLACE_WITH_DB_HOST') }}
DB_USER={{ db_user | default('root') }}
DB_PASS={{ db_pass | default('changeme') }}
DB_NAME={{ db_name | default('movie_analyst') }}
NODE_ENV=production
PORT=3000
